<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Second Express</title>
</head>
<body>
    <body>
        <h1>All To Dos</h1>
        <ul>
          <% artists.forEach(function(t) { %>
            <li>
              <%= artists.artist %>
                - 
              <%= artists.done ? 'done' : 'not done' %>
            </li>
          <% }); %>
        </ul>
      </body>
</body>
</html>